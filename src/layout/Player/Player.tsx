import React from "react";
import { CenterContent, LeftContent, PlayerContent, RightContent } from "./styled";

export const Player = () => {
  return (
    <PlayerContent>
      <LeftContent />
      <CenterContent />
      <RightContent />
    </PlayerContent>
  );
};
